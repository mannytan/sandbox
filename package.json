{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "Sandbox for create a usable three js template",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "dependencies": {
    "express": "^4.18.2",
    "gh-pages": "^6.1.1",
    "three": "^0.163.0"
  }
}
